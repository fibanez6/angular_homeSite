  'use strict';

  describe('myApp.feeder module', function() {

      describe('feeder controller', function(){
          var scope, ctrl, $httpBackend;

          beforeEach(module('myApp.feeder'));

          it('should be defined /feeder', inject(function($controller) {
              //spec body
              expect(ctrl).toBeDefined();
          }));


      });
  });